RewriteEngine On

# For production hosting - redirect to public directory
# RewriteCond %{REQUEST_URI} !^/public/
# RewriteRule ^(.*)$ /public/$1 [L]

# For PHP built-in server development
# The above rules are commented out as they interfere with PHP's built-in server