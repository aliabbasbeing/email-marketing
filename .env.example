# =============================================================================
# EMAIL MARKETING TOOL - CONFIGURATION FILE
# =============================================================================
# This file contains environment-specific configuration settings.
# Copy this file to .env and update the values according to your setup.
# 
# SECURITY WARNING: Never commit your actual .env file to version control!
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Choose your database type: 'mysql' for production, 'sqlite' for development
# 
# For XAMPP/Local Development (Recommended):
# DB_CONNECTION=sqlite
# DB_DATABASE=./storage/database.sqlite
#
# For Production/cPanel Hosting:
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=bulk_mailer
DB_USERNAME=root
DB_PASSWORD=

# Note: For cPanel hosting, your database name and username are usually
# prefixed with your account username (e.g., username_bulk_mailer)

# =============================================================================
# EMAIL SENDING CONFIGURATION (SMTP)
# =============================================================================
# Configure your email sending service. Choose from the options below:

# GMAIL CONFIGURATION (Recommended for small-scale use)
# 1. Enable 2-factor authentication in your Google account
# 2. Generate an "App Password" for this application
# 3. Use the App Password below (not your regular Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-16-character-app-password
SMTP_ENCRYPTION=tls
SMTP_FROM_EMAIL=your-email@gmail.com
SMTP_FROM_NAME="Your Company Name"

# OUTLOOK/HOTMAIL CONFIGURATION
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@outlook.com
# SMTP_PASSWORD=your-password
# SMTP_ENCRYPTION=tls

# CPANEL EMAIL CONFIGURATION (Replace yourdomain.com with your domain)
# SMTP_HOST=mail.yourdomain.com
# SMTP_PORT=587
# SMTP_USERNAME=noreply@yourdomain.com
# SMTP_PASSWORD=your-email-password
# SMTP_ENCRYPTION=tls

# SENDGRID CONFIGURATION (High-volume sending)
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USERNAME=apikey
# SMTP_PASSWORD=your-sendgrid-api-key
# SMTP_ENCRYPTION=tls

# MAILGUN CONFIGURATION (High-volume sending)
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_USERNAME=your-mailgun-smtp-username
# SMTP_PASSWORD=your-mailgun-smtp-password
# SMTP_ENCRYPTION=tls

# =============================================================================
# BACKUP SMTP CONFIGURATION (OPTIONAL)
# =============================================================================
# Configure a backup SMTP service for redundancy
SMTP_BACKUP_HOST=
SMTP_BACKUP_PORT=587
SMTP_BACKUP_USERNAME=
SMTP_BACKUP_PASSWORD=
SMTP_BACKUP_ENCRYPTION=tls
SMTP_BACKUP_FROM_EMAIL=
SMTP_BACKUP_FROM_NAME="Email Marketing Tool Backup"

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Environment: 'development' for XAMPP/local, 'production' for live hosting
APP_ENV=development

# Debug mode: 'true' for development, 'false' for production
# WARNING: Never enable debug mode in production as it exposes sensitive data
APP_DEBUG=true

# Application timezone (see: https://www.php.net/manual/en/timezones.php)
APP_TIMEZONE=UTC

# Application URL (used for generating links in emails)
APP_URL=http://localhost/email-marketing

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================
# CSRF protection token length (16-64 characters recommended)
CSRF_TOKEN_LENGTH=32

# Session lifetime in seconds (7200 = 2 hours)
SESSION_LIFETIME=7200

# Minimum password length for user accounts
PASSWORD_MIN_LENGTH=8

# Force HTTPS (set to 'true' for production with SSL certificate)
FORCE_HTTPS=false

# Enable rate limiting to prevent abuse
RATE_LIMITING=true

# Maximum login attempts before temporary lockout
MAX_LOGIN_ATTEMPTS=5

# Login lockout duration in minutes
LOGIN_LOCKOUT_DURATION=15

# =============================================================================
# EMAIL CAMPAIGN SETTINGS
# =============================================================================
# Maximum emails per batch (to prevent server overload)
EMAILS_PER_BATCH=50

# Delay between email batches in seconds (to comply with rate limits)
BATCH_DELAY=30

# Maximum file upload size in MB
MAX_UPLOAD_SIZE=10

# Allowed file types for uploads (comma-separated)
ALLOWED_UPLOAD_TYPES=csv,txt,xlsx

# =============================================================================
# LOGGING AND MONITORING
# =============================================================================
# Enable detailed logging (recommended for production)
ENABLE_LOGGING=true

# Log level: emergency, alert, critical, error, warning, notice, info, debug
LOG_LEVEL=info

# Enable email bounce tracking
ENABLE_BOUNCE_TRACKING=true

# Enable click tracking in emails
ENABLE_CLICK_TRACKING=true

# Enable open tracking in emails  
ENABLE_OPEN_TRACKING=true

# =============================================================================
# BACKUP CONFIGURATION
# =============================================================================
# Enable automatic database backups
ENABLE_AUTO_BACKUP=false

# Backup frequency: daily, weekly, monthly
BACKUP_FREQUENCY=weekly

# Number of backups to keep
BACKUP_RETENTION=4

# =============================================================================
# ADVANCED SETTINGS
# =============================================================================
# Redis configuration (optional, for session storage and caching)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# Queue system (optional, for background job processing)
QUEUE_DRIVER=database

# Cache driver: file, redis, database
CACHE_DRIVER=file

# =============================================================================
# INSTALLATION NOTES
# =============================================================================
# 
# XAMPP Setup:
# 1. Extract files to: xampp/htdocs/email-marketing/
# 2. Use SQLite database (no MySQL setup required)
# 3. Run: http://localhost/email-marketing/setup-xampp.php
# 
# cPanel Setup:
# 1. Upload files to public_html/ or subdirectory
# 2. Create MySQL database in cPanel
# 3. Run: http://yourdomain.com/setup-cpanel.php
# 
# Manual Setup:
# 1. Copy this file to .env
# 2. Update database and SMTP settings above
# 3. Run: http://yourdomain.com/install.php
# 
# Configuration Wizard:
# For guided setup, visit: http://yourdomain.com/config-wizard.php
# 
# =============================================================================